---
phase: 05-adapters-and-gates
plan: "07"
type: execute
wave: 4
depends_on:
  - "05-04"
  - "05-05"
  - "05-06"
autonomous: false
files_modified: []
requirements:
  - ADAPT-03
  - ADAPT-04
  - EXEC-06
  - EXEC-07
  - SCHEMA-08

must_haves:
  truths:
    - "Full test suite passes with 0 failures"
    - "All 5 requirement IDs have at least one green test covering them"
    - "Gate suspends cascade branch while sibling branches continue"
    - "Email inbound creates intent with sanitized content"
    - "Email outbound sends gate notification with resolve URL"
    - "RBAC blocks unpermitted actors at the API boundary"
    - "resolve_gate fires both pg_notify channels"
  artifacts:
    - path: "tests/test_sanitize.py"
      provides: "4 passing tests — ADAPT-04"
    - path: "tests/test_rbac.py"
      provides: "4 passing tests — SCHEMA-08"
    - path: "tests/test_gate_surfacing.py"
      provides: "4 passing tests — EXEC-06, EXEC-07"
    - path: "tests/test_email_adapter.py"
      provides: "4 passing tests — ADAPT-03, SCHEMA-08"
  key_links:
    - from: "tests/"
      to: "all 5 requirements"
      via: "pytest full suite"
      pattern: "passed"
---

<objective>
Human sign-off: full test suite green, operator manually verifies gate email format.

Purpose: Phase 5 exit gate — all 5 requirement IDs have automated coverage, human confirms email notification is human-readable.
Output: Approved phase 5 checkpoint; ready for phase 6 planning.
</objective>

<execution_context>
@$HOME/.claude/eclusa/workflows/execute-plan.md
@$HOME/.claude/eclusa/templates/summary.md
</execution_context>

<context>
@.eclusa/phases/05-adapters-and-gates/05-CONTEXT.md
@.eclusa/phases/05-adapters-and-gates/05-04-SUMMARY.md
@.eclusa/phases/05-adapters-and-gates/05-05-SUMMARY.md
@.eclusa/phases/05-adapters-and-gates/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full suite green + requirement coverage check</name>
  <files></files>
  <action>
    Run the full test suite and verify each requirement ID has coverage:

    1. Run full suite: `uv run pytest tests/ -v --tb=short`
    2. Verify requirement coverage:
       - ADAPT-03: test_email_adapter.py::test_imap_poll_creates_intent + test_smtp_gate_email_sent + test_message_id_dedup
       - ADAPT-04: test_sanitize.py (all 4) + test_email_adapter.py (sanitize called in inbound path)
       - EXEC-06: test_gate_surfacing.py::test_surface_gate_real_dispatch
       - EXEC-07: test_gate_surfacing.py::test_resolve_gate_fires_notify + test_resolve_gate_fires_scoped_notify
       - SCHEMA-08: test_rbac.py (all 4) + test_email_adapter.py::test_resolve_endpoint_rbac

    3. Run ruff lint check: `uv run ruff check adapters/ executor/dispatch.py executor/loop.py`
    4. Fix any ruff issues (unused imports, line length violations, etc.)

    If any tests fail, diagnose and fix before calling checkpoint.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <done>Full suite shows 0 failures; ruff check exits 0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 5 complete: gate mechanism, email adapter, RBAC, trust boundary.

    - Gate stages suspend their cascade branch while siblings continue
    - Email inbound: IMAP poller creates intent with Message-ID dedup + sanitization gate
    - Email outbound: aiosmtplib sends plain+HTML gate notification with resolve URL
    - RBAC: check_resolve_permission enforces actor permissions at API boundary
    - FastAPI POST /gates/{gate_id}/resolve: 403 for denied, 200 for permitted
    - resolve_gate fires pg_notify on both 'stage_changed' and 'stage_changed:{cascade_id}'
    - Cascade-id-scoped NOTIFY (D-05) implemented in resolve_gate

    All 16 new tests green. Full suite green.
  </what-built>
  <how-to-verify>
    1. Run: `uv run pytest tests/ -q` — confirm 0 failures shown
    2. Run: `uv run pytest tests/test_sanitize.py tests/test_rbac.py tests/test_gate_surfacing.py tests/test_email_adapter.py -v` — confirm all 16 tests pass
    3. Review the email template output:
       ```python
       python3 -c "
       from adapters.protocol import GateContext
       from adapters.email.templates import gate_email_plain
       ctx = GateContext(
           cascade_id='abc-123', stage_id='def-456',
           gate_description='Should we proceed with the v2 API migration?',
           model_recommendation='Yes — all 3 models agree on scope',
           eligible_actor_ids=['you@company.com'],
           resolve_url='http://localhost:8000/gates/def-456/resolve',
       )
       print(gate_email_plain(ctx))
       "
       ```
       Confirm: email is human-readable, contains gate description, model recommendation, and resolve URL.
    4. Check that sibling branch logic works: gate stage blocks only its branch, not all stages in cascade.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -q` exits 0 with 0 failures
- All 5 requirement IDs have green test coverage
- Email template is human-readable (human confirmed)
- ruff check passes on adapter code
</verification>

<success_criteria>
Phase 5 complete when:
- Full test suite green (0 failures)
- 16 new tests across 4 test files all pass
- Human confirms email template is human-readable and contains resolve URL
- ruff exits 0 on adapters/ and modified executor/ files
</success_criteria>

<output>
After completion, create `.eclusa/phases/05-adapters-and-gates/05-07-SUMMARY.md`
</output>
