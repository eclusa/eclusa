---
status: complete
phase: 07.1.1-chat-interface-intent-creation-via-agent-session
source: [07.1.1-CONTEXT.md, git log ea1a9b8..HEAD]
started: 2026-04-06T15:00:00Z
updated: 2026-04-06T15:45:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill all containers. docker compose up -d. All 5 services boot healthy within 60s. localhost:8000 loads login page. localhost:8800/docs loads Swagger UI.
result: pass

### 2. Chat Page Loads
expected: Navigate to /chat. "Operator chat" heading, model selector defaulting to openai:glm-5.1, empty state message, "New thread" badge, input field with placeholder.
result: pass

### 3. Send Message and Get LLM Response
expected: Type message, click Send. Input clears. User message right-aligned. Assistant response streams real-time with typing cursor. Status changes Streaming → Ready, session UUID badge appears.
result: pass

### 4. Cascade Created in Dashboard
expected: After chat message, navigate to /cascades. New cascade with state "active" appears.
result: pass

### 5. Multi-Turn Conversation
expected: Send second message in same chat. Same session UUID. Agent has context from first message.
result: issue
reported: "Navigating away from /chat and returning drops you in a new thread. No session list, no way to see or resume previous chats. Conversation state is ephemeral React state — lost on any page navigation."
severity: major

### 6. Model Selector
expected: Dropdown shows 4+ options including openai:glm-5.1. Changing model sends it to backend. Selector disabled during streaming.
result: pass

### 7. Sessions Page Shows Chat Session
expected: After chatting, navigate to /sessions. Work session appears with transcript content.
result: issue
reported: "Sessions appear in list but clicking them shows 'No transcript messages yet'. Chat message history stored in DB but sessions transcript viewer can't read/display it."
severity: major

### 8. Previous Phase 06 Regressions — Gates Toggle
expected: Gates page renders with correct toggle styling. API responds.
result: issue
reported: "Infinite loading skeleton. API hangs because sessions page WebSocket connections exhaust the DB connection pool (10/10 held by artifact polling). All subsequent API requests deadlock."
severity: blocker

### 9. Previous Phase 06 Regressions — Sessions Page Stability
expected: Sessions page renders without crashing. ErrorBoundary catches issues gracefully.
result: issue
reported: "Sessions page WebSocket connections hold DB pool connections indefinitely. Visiting sessions page with multiple sessions exhausts the pool, deadlocking all other API endpoints. ErrorBoundary catches the React crash but the damage to the backend is already done."
severity: blocker

## Summary

total: 9
passed: 5
issues: 4
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Chat session persists across page navigation with session list and resume"
  status: failed
  reason: "User reported: Navigating away from /chat and returning drops you in a new thread. No session list, no way to see or resume previous chats."
  severity: major
  test: 5
  root_cause: "useChat hook stores state in useState — ephemeral. No session list component, no load-session API call on mount."
  artifacts:
    - path: "ui/src/api/chat.ts"
      issue: "useState for messages/sessionId — lost on unmount"
    - path: "ui/src/pages/ChatPage.tsx"
      issue: "No session sidebar, no session loading on mount"
  missing:
    - "GET /api/chat/sessions endpoint to list user's chat sessions"
    - "Session sidebar in ChatPage showing previous chats"
    - "Load session history from DB when selecting a previous session"

- truth: "Chat transcript visible in Sessions page transcript viewer"
  status: failed
  reason: "User reported: Sessions appear in list but 'No transcript messages yet'. Chat stores in message_history JSONB, transcript viewer reads from artifact table."
  severity: major
  test: 7
  root_cause: "Chat endpoint stores pydantic-ai message history in work_session.message_history JSONB. Sessions transcript viewer reads from artifact table. Two different storage paths — chat never writes artifacts."
  artifacts:
    - path: "adapters/web/api/chat.py"
      issue: "Writes to work_session.message_history, not artifact table"
    - path: "adapters/web/api/sessions.py"
      issue: "Transcript viewer queries artifact table for session messages"
  missing:
    - "Chat endpoint should write each message as an artifact (type=message) so transcript viewer can display them"
    - "OR: transcript viewer should also read from work_session.message_history as fallback"

- truth: "Sessions page WebSocket connections do not exhaust DB connection pool"
  status: failed
  reason: "User reported: Visiting sessions page exhausts pool (10/10 connections held by WebSocket artifact polling), deadlocking all other API endpoints."
  severity: blocker
  test: 8
  root_cause: "WebSocket handler acquires pool connection via pool.acquire() and holds it for the lifetime of the WebSocket. Each session in the list opens a WebSocket, each holds a connection. With pool max_size=10 and multiple sessions, pool exhausted."
  artifacts:
    - path: "adapters/web/api/ws.py"
      issue: "WebSocket handler holds pool connection indefinitely"
  missing:
    - "WebSocket handler must acquire/release connection per poll iteration, not hold for lifetime"
    - "OR: Use LISTEN/NOTIFY instead of polling with held connection"
    - "Consider increasing pool max_size as defense-in-depth"

- truth: "Sessions page renders stably without cascading backend damage"
  status: failed
  reason: "User reported: Sessions page WebSocket connections hold DB pool connections indefinitely, deadlocking all other endpoints."
  severity: blocker
  test: 9
  root_cause: "Same root cause as test 8 — WebSocket pool exhaustion. ErrorBoundary catches React crash but backend damage already done."
  artifacts:
    - path: "adapters/web/api/ws.py"
      issue: "Same as test 8"
  missing:
    - "Same fix as test 8"
