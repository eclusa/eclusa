---
phase: 20-actor-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - adapters/web/api/auth.py
  - adapters/web/chat_bridge.py
autonomous: true
requirements: [ID-01, ID-02, ID-03]

must_haves:
  truths:
    - "POST /api/auth/register with email+name+password creates an actor record with type=human and returns a JWT"
    - "POST /api/auth/token with email+password issues a JWT whose sub claim equals the actor UUID (not the string 'operator')"
    - "verify_token rejects any JWT whose actor_type is absent or whose sub is the string 'operator'"
    - "Tokens issued for agent-type actors carry the agent permission set, not the human permission set"
    - "Calling a permission-checked endpoint with a token that lacks the required permission returns 403"
  artifacts:
    - path: "adapters/web/api/auth.py"
      provides: "register endpoint, reworked token endpoint, per-type permission defaults"
      exports: ["router", "verify_token", "DEFAULT_PERMISSIONS"]
    - path: "adapters/web/chat_bridge.py"
      provides: "ensure_actor that accepts real identity from JWT, not hardcoded 'operator'"
  key_links:
    - from: "POST /api/auth/register"
      to: "actor table"
      via: "INSERT with argon2 password hash"
      pattern: "INSERT INTO actor"
    - from: "POST /api/auth/token"
      to: "JWT sub claim"
      via: "actor.id from DB lookup"
      pattern: "sub.*actor_id"
    - from: "verify_token"
      to: "actor_type enforcement"
      via: "reject sub='operator', require actor_type in payload"
      pattern: "actor_type.*payload"
---

<objective>
Replace the hardcoded "operator" identity in auth.py with real user identity. Add user registration that creates actor records with email, name, and type=human. Rework token issuance to use email+password credentials and embed real actor_id + actor_type in the JWT sub claim. Add per-type default permission sets. Ensure verify_token rejects legacy "operator" tokens.

Purpose: ID-01, ID-02, ID-03 — every JWT carries real user identity; actor types carry enforced permission sets.
Output: Updated auth.py (register + reworked token endpoint + verify_token guard), updated chat_bridge.py ensure_actor.
</objective>

<execution_context>
@$HOME/.claude/eclusa/workflows/execute-plan.md
@$HOME/.claude/eclusa/templates/summary.md
</execution_context>

<context>
@.eclusa/PROJECT.md
@.eclusa/ROADMAP.md
@.eclusa/STATE.md
</context>

<interfaces>
<!-- Current auth.py contracts the executor must change -->

From adapters/web/api/auth.py (current — replace all of this):
```python
class TokenRequest(BaseModel):
    secret: str  # REPLACE: becomes email+password

def _issue_token() -> str:
    payload = {
        "actor_id": "operator",  # REPLACE: becomes real actor UUID
        "exp": ...,
    }

async def issue_token(body: TokenRequest) -> TokenResponse:
    if not secrets.compare_digest(body.secret, get_jwt_secret()):
        ...  # REPLACE: becomes DB lookup + password verification

async def verify_token(...) -> dict:
    # EXTEND: must reject tokens where payload["actor_type"] is missing
    # or payload.get("sub") == "operator"
```

From adapters/web/chat_bridge.py (current ensure_actor — identity is hardcoded):
```python
async def ensure_actor(conn, identity: str, actor_type: str = "human") -> str:
    # identity is "operator" — called from chat.py with the hardcoded string
    # After this phase: called with real email from JWT payload
```

From adapters/web/routes.py (existing RBAC — permissions shape to preserve):
```python
# actor.permissions JSONB shape used by check_resolve_permission:
# {"resolve_gates": ["*"], "view_costs": True}  → human (superuser)
# {"resolve_gates": [], "view_costs": False}     → agent (no gate resolution)
# {} → no permissions
```

From adapters/web/api/chat.py (caller of ensure_actor — must update call site):
# Grep confirm: grep -n "ensure_actor\|operator" adapters/web/api/chat.py
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register endpoint + reworked token endpoint + per-type permission defaults</name>
  <files>adapters/web/api/auth.py</files>
  <behavior>
    - POST /api/auth/register: email (unique), name, password → creates actor with type=human, hashed password stored in actor.permissions JSONB under key "password_hash" (no separate table needed — keep schema zero-migration), returns TokenResponse with JWT
    - Password hashing: use passlib[argon2] (`from passlib.context import CryptContext; pwd_context = CryptContext(schemes=["argon2"])`). Add passlib[argon2] via `uv add passlib[argon2]`.
    - POST /api/auth/token: accepts LoginRequest(email, password) — NOT secret anymore. Looks up actor by identity=email, verifies password hash, issues JWT.
    - JWT payload: `{"sub": actor_id, "email": email, "actor_type": actor_type, "exp": ...}`. No "actor_id" top-level key — sub IS the actor_id. Downstream code reads `payload["sub"]` as actor_id.
    - `verify_token`: after decoding, assert `payload.get("actor_type") is not None` and `payload.get("sub") != "operator"`. If either fails, raise _unauthorized("Legacy token — re-authenticate").
    - DEFAULT_PERMISSIONS: a module-level dict mapping actor_type → default permissions JSONB. human: `{"resolve_gates": ["*"], "view_costs": True}`. agent: `{"resolve_gates": [], "view_costs": False, "spawn_cascades": True}`. system: `{"resolve_gates": ["*"], "view_costs": True, "spawn_cascades": True}`. webhook: `{"resolve_gates": [], "view_costs": False}`.
    - Registration duplicate check: if actor with identity=email already exists, return 409 with detail="Email already registered".
    - No migration needed: actor table already has `identity`, `type`, `permissions` columns (verified in Phase 1 schema). Password hash stored in permissions JSONB.
    - DB access in register/token endpoints: use `request.app.state.pool` (asyncpg) — same pattern as gates.py and routes.py. Add `Request` import.
    - Export `DEFAULT_PERMISSIONS` in `__all__`.
    - Test: tests/test_auth.py — register creates actor, duplicate returns 409, token with wrong password returns 401, issued JWT has sub=actor_uuid (not "operator"), verify_token rejects token with sub="operator"
  </behavior>
  <action>
    1. Run `uv add "passlib[argon2]"` from project root.
    2. Rewrite adapters/web/api/auth.py:
       - Remove `TokenRequest(secret)`, `_issue_token()` with hardcoded "operator"
       - Add `RegisterRequest(email: str, name: str, password: str)`, `LoginRequest(email: str, password: str)`
       - Add `DEFAULT_PERMISSIONS: dict[str, dict]` at module level (human, agent, system, webhook as described in behavior)
       - Add `POST /auth/register` — asyncpg pool from request.app.state.pool; check duplicate by `SELECT id FROM actor WHERE identity = $1`; if exists 409; hash password with pwd_context.hash(); insert actor with type=human, identity=email, permissions=DEFAULT_PERMISSIONS["human"] merged with {"password_hash": hashed_pw}; issue JWT with sub=actor_id, email, actor_type="human"
       - Rewrite `POST /auth/token` — look up actor by identity=email, load permissions, verify pwd_context.verify(password, permissions["password_hash"]), issue JWT
       - Update `_issue_token` to accept actor_id, email, actor_type params; payload = {"sub": actor_id, "email": email, "actor_type": actor_type, "exp": ...}
       - Update `verify_token`: after decode, add guard: if payload.get("sub") == "operator" or "actor_type" not in payload, raise _unauthorized("Legacy token — re-authenticate")
       - Update `__all__` to include `DEFAULT_PERMISSIONS`
    3. Write tests/test_auth.py with the test cases listed in behavior.
    4. Run tests: `pytest tests/test_auth.py -x -v`
  </action>
  <verify>
    <automated>pytest tests/test_auth.py -x -v</automated>
  </verify>
  <done>
    Register endpoint creates actor with type=human; token endpoint accepts email+password; JWT sub is actor UUID; verify_token rejects sub="operator"; all test_auth.py tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ensure_actor and chat.py call site to use real JWT identity</name>
  <files>adapters/web/chat_bridge.py, adapters/web/api/chat.py</files>
  <action>
    1. Grep call sites: `grep -n "ensure_actor\|\"operator\"" adapters/web/api/chat.py adapters/web/api/ws.py` to locate all places that pass hardcoded "operator".
    2. In adapters/web/api/chat.py: endpoints already call verify_token (or should — check). After verifying token, extract actor_id from `payload["sub"]` (not a separate "actor_id" key). Pass this real actor_id directly to `create_chat_cascade` and `create_build_chat_cascade` — skip the ensure_actor call entirely for authenticated users since registration already created the actor.
       - Pattern: `payload = await verify_token(credentials)` → `actor_id = payload["sub"]`
       - Remove or guard any `ensure_actor(conn, "operator")` calls in chat.py
    3. In adapters/web/chat_bridge.py, update `ensure_actor` docstring and default to note that in production it is only called for non-human actors (webhooks, system actors) whose identity is known but not registered via the registration flow. No behavior change needed — the function remains for backward compatibility with non-human actors.
    4. In adapters/web/api/ws.py: same pattern — find any `ensure_actor("operator")` or `actor_id = "operator"` and replace with JWT extraction.
    5. Verify: `grep -rn '"operator"' adapters/web/` should return zero lines in Python files (excluding comments and test fixtures).
  </action>
  <verify>
    <automated>grep -rn '"operator"' adapters/web/ --include="*.py" | grep -v "test_\|#" | wc -l | xargs -I{} test {} -eq 0 && echo "PASS: no hardcoded operator" || echo "FAIL: operator still present"</automated>
  </verify>
  <done>
    No Python source file under adapters/web/ contains the hardcoded string "operator" as an actor identity. Chat and WebSocket endpoints extract actor_id from JWT sub claim.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_auth.py -x -v` all green
- `grep -rn '"operator"' adapters/web/ --include="*.py" | grep -v "#"` returns zero lines
- `curl -X POST http://localhost:8000/api/auth/register -H 'Content-Type: application/json' -d '{"email":"test@example.com","name":"Test User","password":"secret123"}' | jq .` returns access_token
- Decode the token JWT payload: `sub` field is a UUID, not the string "operator"
- `curl -X POST http://localhost:8000/api/auth/token -d '{"email":"test@example.com","password":"wrong"}' -H 'Content-Type: application/json'` returns 401
</verification>

<success_criteria>
1. (ID-01) JWT sub claim is a real actor UUID derived from registered email — the string "operator" never appears as a sub claim in any newly issued token; verify_token actively rejects legacy "operator" tokens with 401
2. (ID-02) POST /api/auth/register creates an actor record with real email as identity, name stored in permissions JSONB, type=human
3. (ID-03) DEFAULT_PERMISSIONS dict maps human/agent/system/webhook to distinct permission sets; these are used at registration time and token issuance; agent tokens cannot resolve gates
</success_criteria>

<output>
After completion, create `.eclusa/phases/20-actor-identity/20-01-SUMMARY.md`
</output>
