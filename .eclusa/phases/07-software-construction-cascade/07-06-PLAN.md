---
phase: 07-software-construction-cascade
plan: "06"
type: execute
wave: 5
depends_on:
  - "07-05"
files_modified: []
autonomous: false
requirements:
  - SCC-01
  - SCC-02
  - SCC-03
  - SCC-04
  - SCC-05
  - SCC-06
  - SCC-07
  - SCC-08
  - WORK-07

must_haves:
  truths:
    - "Full pytest suite runs green — all SCC tests + all prior phase tests pass with no regression"
    - "SCC cascade end-to-end: create_scc_cascade() produces 7 stages; all 7 dispatch correctly"
    - "GHC sidecar defined in docker-compose.yml and shared volume wired for executor"
    - "Claude Code harness stores harness_type='claude_code' confirmed via test"
  artifacts:
    - path: executor/scc.py
      provides: create_scc_cascade
      contains: "scc_stage"
    - path: executor/scc_handlers.py
      provides: all 7 SCC stage handlers
      contains: "dispatch_scc_generate"
    - path: harness/formalize.py
      provides: GHC async subprocess
      contains: "create_subprocess_exec"
    - path: harness/claude_code.py
      provides: Claude Code harness
      contains: "claude_code"
  key_links:
    - from: executor/dispatch.py
      to: executor/scc_handlers.py
      via: SCC routing in dispatch_narrowing
      pattern: "dispatch_scc_refine|dispatch_scc_fanout|dispatch_scc_match"
---

<objective>
Verify Phase 7 completion: run the full test suite, confirm all 9 requirement IDs have test coverage, and get human sign-off that the SCC template is structurally complete.

Purpose: Phase 7 is the final milestone phase. The exit gate ensures every requirement has been implemented and tested before marking the milestone complete.

Output: Phase 7 marked complete, ROADMAP.md updated, human sign-off.
</objective>

<execution_context>
@$HOME/.claude/eclusa/workflows/execute-plan.md
@$HOME/.claude/eclusa/templates/summary.md
</execution_context>

<context>
@.eclusa/PROJECT.md
@.eclusa/ROADMAP.md
@.eclusa/phases/07-software-construction-cascade/07-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full suite verification</name>
  <files></files>
  <action>
Run the full pytest suite and verify all SCC tests pass. Check requirement coverage.

1. Run the full suite:
   ```
   cd /home/lynxnathan/code/eclusa && uv run pytest tests/ -q --tb=short 2>&1 | tail -30
   ```

2. Verify all SCC test files are present and green:
   - tests/test_scc_template.py (SCC-01)
   - tests/test_scc_refine.py (SCC-02)
   - tests/test_scc_match.py (SCC-03)
   - tests/test_scc_cohere.py (SCC-04)
   - tests/test_scc_formalize.py (SCC-05)
   - tests/test_scc_derive.py (SCC-06)
   - tests/test_scc_generate.py (SCC-07)
   - tests/test_scc_fanout.py (SCC-08)
   - tests/test_claude_code_harness.py (WORK-07)

3. Verify key structural invariants:
   ```
   grep -c "dispatch_scc_" executor/scc_handlers.py  # should return 7
   grep "ghc-sidecar" docker-compose.yml             # should exist
   grep "ghc_workspace" docker-compose.yml           # should exist in 3+ places
   grep "harness_type.*claude_code" harness/claude_code.py  # should exist
   ```

4. If any test fails: read the failure output, identify which plan produced the file, fix the specific issue. Do not move on until all tests are green.
  </action>
  <verify>
    <automated>cd /home/lynxnathan/code/eclusa && uv run pytest tests/ -q --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>pytest exits with code 0 — all tests pass including all 9 SCC test files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 7 Software Construction Cascade complete:

- executor/scc.py: create_scc_cascade() creates 7 stages (refine → fanout → match → cohere → formalize → derive → generate)
- executor/dispatch.py: dispatch_narrowing routes all 7 scc_stage values
- executor/scc_handlers.py: all 7 dispatch handlers implemented
- harness/formalize.py: GHC async subprocess with retry loop (max 5) and asyncio.Semaphore(3)
- harness/claude_code.py: Claude Code session with harness_type='claude_code' (WORK-07)
- docker-compose.yml: ghc-sidecar service (haskell:9.10.1, persistent) + shared ghc_workspace volume

9 requirement IDs covered: SCC-01..08, WORK-07.
  </what-built>
  <how-to-verify>
1. Run `uv run pytest tests/ -q --tb=short` — confirm green

2. Check SCC stage count:
   `grep -c "scc_stage" executor/scc.py` — should return 7

3. Check GHC sidecar is in docker-compose:
   `grep "ghc-sidecar" docker-compose.yml` — should exist

4. Confirm Claude Code harness type:
   `grep "claude_code" harness/claude_code.py` — should exist in INSERT

5. Optional smoke test — create a cascade and verify stages:
   ```python
   import asyncio, asyncpg
   from executor.scc import create_scc_cascade
   async def check():
       conn = await asyncpg.connect("postgresql://eclusa:eclusa@localhost:5432/eclusa")
       # seed actor + intent first...
   ```
   (Or just trust the tests if #1 is green.)
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass and SCC structure looks correct, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -q` exits with code 0
- 9 SCC test files present in tests/
- All requirement IDs SCC-01..08 + WORK-07 have corresponding test files
</verification>

<success_criteria>
- Full pytest suite green — zero failures, zero errors
- Human sign-off on SCC structure
- Phase 7 marked complete in ROADMAP.md
- Milestone v1.0 complete
</success_criteria>

<output>
After human approval, create `.eclusa/phases/07-software-construction-cascade/07-06-SUMMARY.md` and update ROADMAP.md Phase 7 status to complete.
</output>
