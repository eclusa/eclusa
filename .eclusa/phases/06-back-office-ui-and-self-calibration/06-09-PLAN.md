---
phase: 06-back-office-ui-and-self-calibration
plan: "09"
type: execute
wave: 5
depends_on:
  - "06-04"
  - "06-05"
  - "06-06"
  - "06-07"
  - "06-08"
files_modified: []
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - CAL-01
  - CAL-02
  - CAL-03
  - CAL-04
  - CAL-05
  - CAL-06
  - CAL-07
  - CAL-08
  - INFRA-01

must_haves:
  truths:
    - "docker-compose up starts all services on a clean host"
    - "Back office UI accessible at http://localhost and shows all 7 nav routes"
    - "API endpoints return data from the running Postgres instance"
    - "All vitest and pytest tests pass"
  artifacts:
    - path: "ui/dist/"
      provides: "Built React SPA (produced by Dockerfile build stage)"
    - path: ".eclusa/phases/06-back-office-ui-and-self-calibration/06-EXIT.md"
      provides: "Phase exit gate record with sign-off"
  key_links:
    - from: "ui nginx.conf /api/ proxy"
      to: "web FastAPI on port 8000"
      via: "Docker network web service name"
      pattern: "proxy_pass.*web:8000"
---

<objective>
Phase 6 exit gate: run the full test suite, confirm all views render with real data, and capture human sign-off. This is the final checkpoint before Phase 7 begins.

Purpose: Verify all 15 requirement IDs are satisfied end-to-end. Gate cannot auto-resolve — requires operator confirmation.
Output: 06-EXIT.md with sign-off, phase marked complete in STATE.md.
</objective>

<execution_context>
@$HOME/.claude/eclusa/workflows/execute-plan.md
@$HOME/.claude/eclusa/templates/summary.md
</execution_context>

<context>
@.eclusa/PROJECT.md
@.eclusa/ROADMAP.md
@.eclusa/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite and build verification</name>
  <files></files>
  <action>
Run the complete test suite and build in sequence:

```bash
# 1. Backend tests
cd /home/lynxnathan/code/eclusa
uv run pytest tests/ -v --tb=short -q 2>&1 | tail -30

# 2. Frontend tests
cd /home/lynxnathan/code/eclusa/ui
npx vitest run --reporter=verbose 2>&1 | tail -30

# 3. Frontend build
npm run build 2>&1 | tail -15

# 4. Compose config validation
cd /home/lynxnathan/code/eclusa
docker compose config --quiet && echo "COMPOSE_VALID"
```

Report results. If any test fails, create an issue note but do NOT block checkpoint — let the human decide.
  </action>
  <verify>
    <automated>cd /home/lynxnathan/code/eclusa && uv run pytest tests/ -q --tb=short 2>&1 | tail -10 && cd ui && npx vitest run 2>&1 | tail -10 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Backend tests pass; vitest tests pass; npm run build exits 0; docker compose config is valid.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 6 back office UI and self-calibration:
- Plan 01: JWT auth + cascades + gates FastAPI endpoints
- Plan 02: Vite + React 19 + shadcn/ui scaffold, AppShell/Sidebar, Dockerfile
- Plan 03: Sessions, costs, ledger, knowledge, metrics, WebSocket endpoints
- Plan 04: DashboardPage (UI-01) + GatesPage (UI-02)
- Plan 05: SessionPage (UI-03) + CostsPage (UI-04)
- Plan 06: LedgerPage (UI-05) + KnowledgePage (UI-06)
- Plan 07: MetricsPage (CAL-01..08)
- Plan 08: Full docker-compose with all 5 services (INFRA-01)
  </what-built>
  <how-to-verify>
Option A — Docker compose (full):
1. `cd /home/lynxnathan/code/eclusa && docker compose up --wait`
2. Visit http://localhost — confirm UI loads with Sidebar and nav links
3. Check each route: /cascades, /gates, /sessions, /costs, /ledger, /knowledge, /metrics
4. On /metrics — confirm "Requires 10+ resolved gates" empty states
5. On /ledger — confirm AS OF picker with 4 preset buttons
6. On /knowledge — confirm search bar and Communities tab
7. `docker compose down`

Option B — Development mode (faster if Docker is slow):
1. `cd /home/lynxnathan/code/eclusa && uv run uvicorn adapters.web.app:create_app --factory --port 8000 --reload`
2. `cd ui && npm run dev`
3. Visit http://localhost:5173 and verify same checklist above
4. On /gates — create a test gate in DB and verify it appears with resolve form
  </how-to-verify>
  <resume-signal>Type "approved" if all views load and the test suite passed. Describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Phase 6 complete when:
- [ ] All backend pytest tests pass
- [ ] All vitest tests pass
- [ ] npm run build succeeds
- [ ] docker compose config validates
- [ ] Human confirmed all 7 views render
- [ ] 06-EXIT.md created with sign-off
</verification>

<success_criteria>
- UI-01 through UI-06: all 6 UI views implemented and rendering
- CAL-01 through CAL-08: all 8 metric cards with null/value states
- INFRA-01: docker-compose up boots full instance
- Full test suite green
- Human sign-off recorded in 06-EXIT.md
</success_criteria>

<output>
After completion, create:
1. `.eclusa/phases/06-back-office-ui-and-self-calibration/06-09-SUMMARY.md`
2. `.eclusa/phases/06-back-office-ui-and-self-calibration/06-EXIT.md` with:
   - Date
   - Operator sign-off
   - Any issues noted
   - Phase marked complete
3. Update STATE.md: phase 6 complete, position = "Phase 7 ready"
4. Update ROADMAP.md: Phase 6 checkbox ticked, plans list filled in
</output>
