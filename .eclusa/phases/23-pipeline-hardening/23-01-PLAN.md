---
phase: 23-pipeline-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - executor/scc_handlers.py
  - tests/test_scc_fanout.py
autonomous: true
requirements:
  - PIPE-02

must_haves:
  truths:
    - "A fanout stage with scope_doc in its input (build-mode v2) executes without error — no refine_stage_id needed"
    - "A fanout stage with only refine_stage_id (legacy v1) still executes correctly by fetching scope_doc from the referenced stage"
    - "A fanout stage with neither scope_doc nor refine_stage_id logs an error and returns without crashing"
  artifacts:
    - path: "executor/scc_handlers.py"
      provides: "handle_intent_validation_fanout with branching input logic"
      contains: "scope_doc = stage_input.get"
    - path: "tests/test_scc_fanout.py"
      provides: "unit tests for both input paths"
      exports: ["test_fanout_uses_scope_doc_directly", "test_fanout_falls_back_to_refine_stage_id"]
  key_links:
    - from: "executor/scc_handlers.py handle_intent_validation_fanout"
      to: "run_fan_out_with_db"
      via: "prepared_context built from scope_doc regardless of source"
      pattern: "scope_doc.*stage_input.get"
---

<objective>
Fix `handle_intent_validation_fanout` to branch on input type: if `scope_doc` is present in the stage input (build-mode v2 from `create_scc_cascade_from_refine`), use it directly. If not, fall back to fetching it via `refine_stage_id` (legacy v1 from `create_scc_cascade`). This unblocks the SCC pipeline end-to-end.

Purpose: The current handler always requires `refine_stage_id` (lines 320-323 of scc_handlers.py), which means cascades created via the Refine agent conversation path (build-mode v2) fail immediately — they have `scope_doc` directly in the fanout stage input, not a pointer. This is the critical path blocker for PIPE-01.

Output: Updated `handle_intent_validation_fanout`, updated fanout tests covering both input paths.
</objective>

<execution_context>
@$HOME/.claude/eclusa/workflows/execute-plan.md
@$HOME/.claude/eclusa/templates/summary.md
</execution_context>

<context>
@.eclusa/PROJECT.md
@.eclusa/ROADMAP.md
@.eclusa/STATE.md

@executor/scc_handlers.py
@executor/scc.py
@tests/test_scc_fanout.py
</context>

<interfaces>
<!-- Current scc_handlers.py handle_intent_validation_fanout — lines 314-374 -->
<!-- Key issue: lines 320-323 bail out with logger.error when refine_stage_id is missing -->
<!-- Fix: check scope_doc first, only fall back to DB lookup if scope_doc absent -->

From executor/scc.py — create_scc_cascade (v1, 7-stage):
  fanout stage input = { "scc_stage": "intent_validation_fanout", "refine_stage_id": refine_id }
  — no scope_doc in input; scope_doc fetched from refine stage output

From executor/scc.py — create_scc_cascade_from_refine (v2, 6-stage):
  fanout stage input = { "scc_stage": "intent_validation_fanout", "scope_doc": scope_doc, "raw_intent": scope_doc }
  — scope_doc present directly; no refine_stage_id at all

From executor/scc_handlers.py — _normalize_stage_input:
  normalizes stage["input"] (str or dict) into a plain dict

From executor/scc_handlers.py — _json_or_value, _stage_input_value:
  helpers for extracting typed values from jsonb stage inputs
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix fanout handler — branch on scope_doc vs refine_stage_id</name>
  <files>executor/scc_handlers.py, tests/test_scc_fanout.py</files>
  <behavior>
    - Test 1: fanout stage with {"scc_stage": "intent_validation_fanout", "scope_doc": "direct scope"} calls run_fan_out_with_db with prepared_context containing "direct scope" — no DB fetch of refine stage
    - Test 2: fanout stage with {"scc_stage": "intent_validation_fanout", "refine_stage_id": "<id>"} fetches scope_doc from the referenced stage's output (existing behavior, must still pass)
    - Test 3: fanout stage with neither scope_doc nor refine_stage_id logs an error and returns early — no crash, no call to run_fan_out_with_db
  </behavior>
  <action>
In `executor/scc_handlers.py`, rewrite `handle_intent_validation_fanout` starting at line 319.

Replace:
```python
refine_stage_id = stage_input.get("refine_stage_id")
if not refine_stage_id:
    logger.error("Fan-out stage %s missing refine_stage_id", stage["id"])
    return
```

With the branching logic:
1. First check `scope_doc = stage_input.get("scope_doc")` — if truthy, use it directly (build-mode v2 path). Skip all DB fetching of refine stage output.
2. Else check `refine_stage_id = stage_input.get("refine_stage_id")` — if truthy, fetch scope_doc from the refine stage's output column (existing DB query). This is the legacy v1 path.
3. Else: `logger.error("Fan-out stage %s has neither scope_doc nor refine_stage_id", stage["id"]) ; return`

The `raw_intent` resolution logic below (lines 339-354) is unchanged — it still falls back to DB lookup when not in stage_input.

In `tests/test_scc_fanout.py`, add:
- `test_fanout_uses_scope_doc_directly`: seed a fanout stage with `scope_doc` in its input (no `refine_stage_id`), mock `run_fan_out_with_db`, assert it was called with the scope_doc in `prepared_context` and no DB fetch for refine stage output occurred
- `test_fanout_falls_back_to_refine_stage_id`: the existing `_seed_fanout_stage` helper already seeds the legacy path — verify existing tests still pass without modification
- `test_fanout_missing_both_inputs_returns_early`: stage with neither, assert `run_fan_out_with_db` was NOT called
  </action>
  <verify>
    <automated>cd /home/lynxnathan/code/eclusa && python -m pytest tests/test_scc_fanout.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>All fanout tests pass. Both input paths (scope_doc direct and refine_stage_id DB lookup) are covered. Missing-both-inputs returns cleanly. No regression in existing fanout tests.</done>
</task>

</tasks>

<verification>
```bash
cd /home/lynxnathan/code/eclusa && python -m pytest tests/test_scc_fanout.py -v 2>&1 | tail -30
```
All tests green. No import errors in executor/scc_handlers.py:
```bash
cd /home/lynxnathan/code/eclusa && python -c "from executor.scc_handlers import handle_intent_validation_fanout; print('ok')"
```
</verification>

<success_criteria>
- `scope_doc` present in stage input → fanout proceeds without any DB lookup for refine stage
- `refine_stage_id` present, no `scope_doc` → fanout fetches scope_doc from refine stage output (legacy path preserved)
- Neither present → error logged, function returns, no crash
- `python -m pytest tests/test_scc_fanout.py` exits 0
</success_criteria>

<output>
After completion, create `.eclusa/phases/23-pipeline-hardening/23-pipeline-hardening-01-SUMMARY.md`
</output>
