---
phase: "07.1"
plan: "07.1-02"
name: Frontend dependency upgrade + TypeScript clean build
wave: 1
depends_on: []
autonomous: true
requirements:
  - INFRA-01
files_modified:
  - ui/package.json
  - ui/package-lock.json
  - ui/vite.config.ts
  - ui/tsconfig.json
  - ui/tsconfig.app.json
---

# Plan 07.1-02: Frontend Dependency Upgrade + TypeScript Clean Build

## Objective

Replace vendor/ local deps with npm registry versions. Upgrade all frontend deps to latest stable. Fix vite.config.ts for vite 8. Fix all TypeScript errors. `npm run build` succeeds.

## Tasks

<task id="1" type="auto">
<action>
1. cd ui/

2. Remove vendor deps from package.json — replace file: references with registry versions:
   - "@tanstack/react-query": "file:./vendor/react-query" → "@tanstack/react-query": "^5.96.0"
   - "react-use-websocket": "file:./vendor/react-use-websocket" → "react-use-websocket": "^4.13.0"
   - "recharts": "file:./vendor/recharts" → "recharts": "^2.15.0"

3. Remove vendor/ directory: `rm -rf vendor/`

4. Run `npm install` — generates fresh lockfile from registry

5. Fix vite.config.ts: the `test` property was removed but vitest needs a separate vitest.config.ts:
   Create ui/vitest.config.ts:
   ```ts
   import { defineConfig } from 'vitest/config'
   export default defineConfig({
     test: {
       globals: true,
       environment: 'node',
       setupFiles: './src/test-setup.ts',
     },
   })
   ```

6. Run `npx tsc -b 2>&1 | head -30` — fix any TypeScript errors

7. Run `npx vite build` — must succeed and produce dist/

8. Run `npx vitest run 2>&1 | tail -10` — check test status
</action>
<verify>
<automated>cd /home/lynxnathan/code/eclusa/ui && npx vite build 2>&1 | tail -5</automated>
</verify>
<done>vite build succeeds, dist/ exists</done>
</task>
