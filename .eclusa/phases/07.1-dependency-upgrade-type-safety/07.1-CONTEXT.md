# Phase 7.1: Dependency Upgrade + Type Safety - Context

**Gathered:** 2026-04-05
**Status:** Ready for planning

<domain>
## Phase Boundary

Upgrade all Python and JavaScript dependencies to latest stable versions. Fix all pyright type errors and vite 8 / TypeScript compilation errors. Run ruff clean. This is a polish/cleanup phase before UAT — no new features.

</domain>

<decisions>
## Implementation Decisions

### Python backend
- **D-01:** Upgrade all deps in pyproject.toml to latest stable via `uv add`
- **D-02:** Run `uv run pyright --warnings` and fix all type errors
- **D-03:** Run `uv run ruff check . && uv run ruff format .` clean
- **D-04:** Run full pytest suite green after all upgrades

### JavaScript frontend
- **D-05:** Upgrade all deps in ui/package.json to latest stable — vite 8 already pinned
- **D-06:** Remove vendor/ directory — install from npm registry instead of local files
- **D-07:** Fix all TypeScript compilation errors (`tsc -b`)
- **D-08:** Run `npm run build` clean (vite build succeeds)

### Use Codex (gpt-5.4-mini) for all executor work
- **D-09:** All executor subagents use codex:codex-rescue subagent type

### Claude's Discretion
- Exact version pins (always latest stable)
- Whether to split into separate Python/JS plans or combine

</decisions>

<deferred>
## Deferred Ideas

None

</deferred>

---

*Phase: 07.1-dependency-upgrade-type-safety*
*Context gathered: 2026-04-05*
