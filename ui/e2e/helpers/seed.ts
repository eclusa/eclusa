export async function seedTestData(baseURL: string): Promise<void> {
  // Placeholder for future fetch-based test seeding.
  // Keeping the signature in place makes it easy to add setup calls later
  // without changing the test files.
  void baseURL
}
